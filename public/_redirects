# Client-side routing (ADR-0004): every address is the SPA's to resolve, so a
# direct visit or reload on /profile, /catalog, /login … serves index.html
# instead of Netlify's 404. Real files in dist/ still win over this rule.
/*  /index.html  200
