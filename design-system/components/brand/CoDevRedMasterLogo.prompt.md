The codev wordmark in brand red — use wherever the parent company signs a surface.

```jsx
<CoDevRedMasterLogo height={39} />
```

Native artwork is 142×39 raster (assets/logo-codev-red.png). Use `CoDevWhiteMasterLogo` on dark or photographic grounds, and `CoDevSupplyRequestsLogo` for the product lockup.
