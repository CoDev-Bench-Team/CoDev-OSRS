The codev wordmark in white — for dark or photographic grounds only.

```jsx
<CoDevWhiteMasterLogo height={39} />
```

Same artwork as `CoDevRedMasterLogo`, knocked out. Never place it on the light canvas.
