The one search field in OSRS — sits directly above the catalog grid or a data table, stretching the content width.

```jsx
<Search placeholder="Search inventory by item name or code" style={{ flexGrow: 1 }} />
```

46px tall, 10px radius, white fill with a 1px `--border-default` inset ring, 18px magnifier at `--text-secondary`, Inter 400 / 14px placeholder. No size or state variants exist in the source.
