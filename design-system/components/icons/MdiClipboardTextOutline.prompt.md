Outline clipboard-with-text glyph, 24×24 — heavier alternate of the request-list mark.

```jsx
<MdiClipboardTextOutline size={24} color="var(--brand-primary)" />
```

Figma source: `mdi:clipboard-text-outline` (node 15:539). Paints with `currentColor`; no variants.
