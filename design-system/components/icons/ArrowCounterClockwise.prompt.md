Counter-clockwise refresh arrow, 30×30 frame — the regenerate affordance.

```jsx
<ArrowCounterClockwise size={24} color="var(--brand-primary)" />
```

Figma source: `arrow-counter-clockwise` (node 30:3067). Paints with `currentColor`; no variants.
