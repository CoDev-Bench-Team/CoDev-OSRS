Thin right chevron, 30×30 frame — forward navigation and disclosure.

```jsx
<CaretRight size={24} color="var(--brand-primary)" />
```

Figma source: `caret-right` (node 30:3083). Paints with `currentColor`; no variants.
