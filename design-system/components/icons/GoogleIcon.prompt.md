Google's "G" mark at the three sizes the file defines — only used inside `SignInButton`.

```jsx
<GoogleIcon size="40x40" />
```

Variants: `32x32` (default), `40x40`, `48x48`. It is a third-party brand asset: do not recolour or crop it.
