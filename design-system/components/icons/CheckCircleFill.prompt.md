Filled check-in-circle glyph, 30×30 frame — confirmation and saved states.

```jsx
<CheckCircleFill size={24} color="var(--brand-primary)" />
```

Figma source: `check-circle-fill` (node 30:3086). Paints with `currentColor`; no variants.
