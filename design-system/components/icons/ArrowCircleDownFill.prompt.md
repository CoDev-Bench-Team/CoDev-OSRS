Filled down-arrow-in-circle glyph, 30×30 frame.

```jsx
<ArrowCircleDownFill size={24} color="var(--brand-primary)" />
```

Figma source: `arrow-circle-down-fill` (node 30:3080). Paints with `currentColor`; no variants.
