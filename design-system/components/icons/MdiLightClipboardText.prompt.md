Light-weight clipboard-with-text glyph, 24×24 — the Request List marker in the top bar.

```jsx
<MdiLightClipboardText size={24} color="var(--brand-primary)" />
```

Figma source: `mdi-light:clipboard-text` (node 17:23). Paints with `currentColor`; no variants.
